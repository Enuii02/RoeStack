extends layout
block content
    - console.log(data)
    .main-content 
    h1 All students 
    table(border=1)
        for item in data 
            tr  
                td #{item.id}
                td
                    a(href='/user-single/' + item.id) #{item.name}
                td 
                    input(type="checkbox", name="")
