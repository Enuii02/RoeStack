extends ../layout
block content 
    h2(style="font-size: 32px; margin-bottom: 50px;") Explore Communities 
    .community-grid 
        each community in content.communityList
            include ../partials/community-card.pug